#include <stdio.h>

int main() {
    double area, a;

    printf("Enter the a of the square: ");
    scanf("%lf", & a );

    area = a * a ;

    printf("The area of the square is: %lf\n", area);

    return 0;
}