#include <stdio.h>

int main() {
    double a, A;

    printf("Enter the a of the cube: ");
    scanf("%lf", & a );

    A = 6 * a * a;

    printf("The surface area of the cube is: %lf\n", A);

    return 0;
}
